# Test Database Configuration
DATABASE_URL="postgresql://txai_user:txai_password@localhost:5433/txai_support_test"

# Test Environment Variables
NODE_ENV=test
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# WhatsApp Test Configuration (optional for unit tests)
WHATSAPP_SESSION_PATH=./sessions-test
WHATSAPP_TOKEN_PATH=./tokens-test

# Upload Test Configuration
UPLOAD_DIR=./uploads-test
